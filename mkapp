
#!/bin/bash

set -e
set -x

env

. /usr/share/mkapp/functions

set_env $ARCH

apt-get update
apt-get install -y cmake pkg-config libxml2-dev:$ARCH libssl1.0-dev:$ARCH libssl1.1 openssl libc6-dev-armhf-cross

ant_build

if [ "$PUBLISH" ]; then
  publish_ivy ivy
fi
